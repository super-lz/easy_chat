# Local listen port
PORT=8787

# Allowed browser origin. Use * only for local experiments.
ALLOW_ORIGIN=*

# Public base URL written into QR codes as serverUrl.
# Set this in deployed environments behind a reverse proxy or custom domain.
PUBLIC_SERVER_URL=
