# Browser-visible pairing API base URL.
# Leave empty for same-origin /api access.
VITE_PAIRING_API_URL=

# Dev-only Vite proxy target for /api and /health.
PAIRING_SERVICE_PROXY_TARGET=http://127.0.0.1:8787
